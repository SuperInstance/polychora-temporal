[package]
name = "polychora-temporal-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
polychora = { path = "../polychora" }
common = { path = "../../common", features = ["ndarray", "nalgebra"] }
higher-dimension-playground = { path = "../.." }
serde = { version = "1.0", features = ["derive"] }
log = "0.4"
